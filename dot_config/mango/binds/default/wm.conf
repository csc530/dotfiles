bind=SUPER,r,reload_config

# exit
bind=CTRL+ALT,delete,quit
bind=SUPER,c,killclient,


# switch layout
bind=SUPER,space,switch_layout

# tag switch
# bind=SUPER,tab,view,-1,0
bind=SUPER,left,viewtoleft_have_client,0
bind=SUPER,right,viewtoright_have_client,0
## static tags
bind=SUPER,1,view,1,0
bind=SUPER,2,view,2,0
bind=SUPER,3,view,3,0
bind=SUPER,4,view,4,0
bind=SUPER,5,view,5,0
bind=SUPER,6,view,6,0
bind=SUPER,7,view,7,0
bind=SUPER,8,view,8,0
bind=SUPER,9,view,9,0

# tag: move client to the tag and focus it
bind=SUPER+SHIFT,Left,tagtoleft,0
bind=SUPER+SHIFT,Right,tagtoright,0
# tagsilent: move client to the tag and not focus it
# bind=Alt,1,tagsilent,1
bind=SUPER+SHIFT,1,tag,1,0
bind=SUPER+SHIFT,2,tag,2,0
bind=SUPER+SHIFT,3,tag,3,0
bind=SUPER+SHIFT,4,tag,4,0
bind=SUPER+SHIFT,5,tag,5,0
bind=SUPER+SHIFT,6,tag,6,0
bind=SUPER+SHIFT,7,tag,7,0
bind=SUPER+SHIFT,8,tag,8,0
bind=SUPER+SHIFT,9,tag,9,0


# switch window focus
bind=SUPER,Tab,spawn,~/.config/mango/scripts/toggle-tag-view.nu right
bind=SUPER+SHIFT,Tab,spawn,~/.config/mango/scripts/toggle-tag-view.nu left
bind=ALT,Tab,focusstack,next
bind=ALT+SHIFT,Tab,focusstack,prev
bind=ALT,Left,focusdir,left
bind=ALT,Right,focusdir,right
bind=ALT,Up,focusdir,up
bind=ALT,Down,focusdir,down

# swap window
bind=SUPER+SHIFT,Up,exchange_client,up
bind=SUPER+SHIFT,Down,exchange_client,down
bind=SUPER+SHIFT,Left,exchange_client,left
bind=SUPER+SHIFT,Right,exchange_client,right


# movewin
bind=CTRL+ALT,Up,movewin,+0,-50
bind=CTRL+ALT,Down,movewin,+0,+50
bind=CTRL+ALT,Left,movewin,-50,+0
bind=CTRL+ALT,Right,movewin,+50,+0

# resizewin
bind=CTRL+SHIFT,Up,resizewin,+0,-50
bind=CTRL+SHIFT,Down,resizewin,+0,+50
bind=CTRL+SHIFT,Left,resizewin,-50,+0
bind=CTRL+SHIFT,Right,resizewin,+50,+0
